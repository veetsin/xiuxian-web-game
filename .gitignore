# 本地开发 / 编辑器
.claude/
.DS_Store
*.log
Thumbs.db

# 设计文档 / 开发工具：只留本地，绝不进公开仓库。
# 公开仓库只含游戏本体：index.html / css / js / README.md / .gitignore。
# 即便误用 `git add .`，下列内容也会被忽略，不会被加入提交。
docs/
tools/
AGENT_CONTRACT.md
xiuxian_horizontal_expansion_spec.md
*_spec.md
*_SPEC.md

# Node（本项目零依赖，仅防御）
node_modules/
npm-debug.log*
